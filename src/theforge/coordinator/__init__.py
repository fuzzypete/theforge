"""theforge.coordinator — deterministic state-machine orchestrator package."""

# Public entry points live in submodules; callers import from those directly.
# e.g. from theforge.coordinator.engine import run_task
__all__: list[str] = []
