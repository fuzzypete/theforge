gate_decision: PASS
dev_notes: |
  summary: "Added WHAT-not-HOW constraints to both synthesis and single-model prompts in
    ideate.py. Both _build_synthesis_prompt and _build_single_model_prompt now instruct
    the LLM to produce behavioral acceptance criteria, explicitly prohibit function
    signatures, dataclass definitions, code snippets, file-internal steps, and variable
    names, and target ≤150 lines for the spec body. The prohibition text is scoped to the
    SPEC output section in the single-model prompt so ideation thinking remains
    unconstrained. _build_phase1_prompt is untouched. test_synthesis_prompt_includes_all_outputs
    gained four new assertions checking for prohibition keywords and behavioral AC instruction
    text. A new round-trip test (test_round_trip_ideate_to_dev_prompt) calls run_ideation
    with a mocked single-model agent, parses the written spec via task.py's
    parse_spec_frontmatter, constructs a TaskSpec, calls build_dev_prompt, and asserts
    the result is non-empty and contains the spec title."
  commits:
    - sha: "234f538"
      message: "feat(ideate): lean story output — behavioral ACs, no implementation detail"
  acceptance_criteria:
    - criterion: "forge ideate output contains no function signatures or code blocks"
      status: MET
      notes: "Prohibition text added to both _build_synthesis_prompt and _build_single_model_prompt. LLM compliance depends on the model following instructions; prompt-level enforcement is the correct mechanism here."
    - criterion: "Acceptance criteria in generated stories describe observable behavior not internal structure"
      status: MET
      notes: "Both prompts now include 'observable behavior' instruction and examples of what is prohibited."
    - criterion: "file_scope is still populated in frontmatter"
      status: MET
      notes: "Template unchanged in both prompts; file_scope remains in the frontmatter block."
    - criterion: "Phase 1 ideation prompts are unchanged"
      status: MET
      notes: "_build_phase1_prompt not touched."
    - criterion: "Single-model path produces the same lean format"
      status: MET
      notes: "Same prohibition and behavioral AC instructions added to _build_single_model_prompt, scoped to the SPEC output section."
    - criterion: "Generated stories are under 150 lines"
      status: MET
      notes: "Brevity target (≤150 lines) added to both prompts. Actual LLM compliance is advisory; no post-processing enforcer is in scope."
    - criterion: "Existing ideate tests pass or are updated to match new output format"
      status: MET
      notes: "test_synthesis_prompt_includes_all_outputs updated with four new assertions. All 674 tests pass."
    - criterion: "A round-trip test: forge ideate → forge run --dry-run on the output produces a valid dev prompt"
      status: MET
      notes: "test_round_trip_ideate_to_dev_prompt calls run_ideation, then parse_spec_frontmatter + build_dev_prompt from task.py, asserting non-empty result containing the spec title."
  spec_deviations: none
  deferred_items: none
  gate_result: PASS
