gate_decision: PASS
scope_completed:
  - P1: Reviewer budget clamping — max(remaining, 0)/pool_size prevents exceeding budget_usd
  - P1: review_pool override in smart-config mode — profiles.review_pool entries matched by name
  - P1: large complexity materializes synthesis for 2-model pools (was skipped, now always created)
  - P1: complexity parsed on all successful preflights; adaptive swapping still gated on smart_config_models
  - Tests for all four P1 fixes (11 new test methods across test_config.py and test_coordinator.py)
deferred_followups:
  - P2: large complexity preserves original dev budget (may be inadequate for stronger model)
  - P2: _find_registry_info_for_profile linear scan (minor perf, registry is tiny)
next_recommended_step: Submit for review
