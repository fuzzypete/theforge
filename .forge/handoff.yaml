dev_notes: "summary: \"Completed the V0.2.2 review-fix iteration by tightening nearby-line\
  \ corroboration so it cannot chain transitively across distant findings, extending\
  \ plan-agent-review model conflict checks to adaptive planner selections, preserving\
  \ the API-provider default-tools behavior from the prior pass, and adding regression\
  \ coverage for the new paths.\"\ncommits:\n  - sha: \"PENDING\"\n    message: \"\
  fix(v0.2.2): address review findings (iter 1)\"\nacceptance_criteria:\n  - criterion:\
  \ \"Two findings at the same file and line (within ±3 lines) are corroborated even\
  \ if their descriptions differ\"\n    status: MET\n    notes: \"Nearby-line corroboration\
  \ still groups same-file findings within the ±3-line window even when the descriptions\
  \ differ, and the grouping now requires all merged reports to remain within that\
  \ same window.\"\n  - criterion: \"Exact-match fingerprinting continues to work\
  \ for identical findings\"\n    status: MET\n    notes: \"The exact fingerprint\
  \ path remains intact for identical findings, and the updated finding-classifier\
  \ tests keep that behavior covered.\"\n  - criterion: \"`load_config()` raises `ValueError`\
  \ if any plan_agent_review pool member uses the same model as the planner\"\n  \
  \  status: MET\n    notes: \"Config loading now rejects planner/model reuse for\
  \ plan review in the explicit planner path, the legacy scalar path, and the adaptive\
  \ planner path when assignment can select the same model at runtime.\"\n  - criterion:\
  \ \"Error message names the conflicting model\"\n    status: MET\n    notes: \"\
  The config validation error includes the reused model name so the misconfiguration\
  \ is explicit.\"\n  - criterion: \"Test covers the invalid config case\"\n    status:\
  \ MET\n    notes: \"Regression coverage in `tests/test_config.py` exercises explicit-planner\
  \ overlap, legacy overlap, and adaptive-planner overlap, and preserves an allowed\
  \ different-model case.\"\n  - criterion: \"API-provider profiles with no explicit\
  \ `allowed_tools` default to `(\\\"read_file\\\", \\\"bash\\\", \\\"glob\\\", \\\
  \"grep\\\", \\\"submit_review\\\")`\"\n    status: MET\n    notes: \"Provider-backed\
  \ reviewer profiles now resolve to the required read-only default tool tuple when\
  \ `allowed_tools` is omitted.\"\n  - criterion: \"CLI-provider profiles continue\
  \ to use their existing defaults\"\n    status: MET\n    notes: \"The API-default\
  \ change is limited to provider-backed profiles, so CLI reviewer profiles continue\
  \ to inherit their existing default tools.\"\n  - criterion: \"Test covers an API-provider\
  \ profile with no `allowed_tools` set\"\n    status: MET\n    notes: \"Tests now\
  \ cover provider profiles without `allowed_tools` in the parsed-profile, override,\
  \ and legacy `plan_agent_review.profiles` paths.\"\nstory_deviations: none\ndeferred_items:\
  \ none\ngate_result: PASS\n"
gate_decision: PASS
