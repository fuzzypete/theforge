gate_decision: PASS
dev_notes: |
  summary: "Fixed two unsatisfied acceptance criteria in coord_notify.py: raised the
    urlopen timeout in _ntfy_publish from 10s to 15s (now within the 15-30s required range),
    and replaced the bare except pass with a warning log via _cu._log so notification
    failures are visible in the forge output. Updated the one test that was asserting
    timeout==10 to assert timeout==15. No new dependencies introduced. All other ACs
    (gh subprocess timeout, PR failure logging) were already satisfied per preflight."
  commits:
    - sha: "55a2abc"
      message: "fix(coord_notify): raise ntfy timeout to 15s and log failures as warnings"
  acceptance_criteria:
    - criterion: "All urllib.request.urlopen calls in coord_notify.py have an explicit timeout= parameter (connect + read, 15-30s)"
      status: MET
      notes: "coord_notify.py line 199 now uses timeout=15. Lines 217, 265, 315 already used timeout=15."
    - criterion: "_ntfy_publish is wrapped in try/except so notification failures log a warning and continue — never block the sprint"
      status: MET
      notes: "except clause now calls _cu._log(f'WARNING: ntfy publish failed (continuing): {exc}') then falls through. Pipeline continues."
    - criterion: "Any subprocess calls to gh (PR creation, issue filing) have a timeout"
      status: MET
      notes: "Already satisfied (coord_phases.py:212-218 uses timeout=60). No changes needed."
    - criterion: "Notification and PR creation failures are logged at WARNING level with the error detail, then the pipeline continues"
      status: MET
      notes: "ntfy: now logs via _cu._log with 'WARNING:' prefix and exc detail. PR creation: coord_phases.py already used _pr_log.warning() with error detail."
    - criterion: "No new dependencies introduced"
      status: MET
      notes: "Only stdlib and existing _cu._log used."
    - criterion: "Existing tests pass"
      status: MET
      notes: "1189 tests pass (1 test updated to reflect new timeout value)."
  story_deviations: none
  deferred_items: none
  gate_result: PASS
