dev_notes: |
  summary: "Redesigned sprint resume merge detection so squash-merged stories consult persisted APPROVE audit history without the stale-branch guard used for ordinary resume triage, collapsed per-story audit generation to a single guarded call that also appends sprint runs to history.jsonl, and tightened regression coverage around real squash-merge topology plus the expected stale/no-worktree resume reasons."
  commits:
    - sha: "12a8e9f"
      message: "test(sprint): align resume stale triage expectations"
    - sha: "8890eeb"
      message: "fix(sprint): address review findings (iter 1)"
    - sha: "05c48cc"
      message: "fix(sprint): address review findings (iter 1)"
    - sha: "eb4fdb5"
      message: "fix(sprint): clarify approve-based merged triage"
    - sha: "31219e5"
      message: "fix(sprint): address review findings (iter 1)"
    - sha: "55b962e"
      message: "fix(sprint): persist resume audit history"
  acceptance_criteria:
    - criterion: "Sprint resume should not re-run stories from scratch when they were already squash-merged."
      status: MET
      notes: "Merged detection now uses persisted APPROVE audit history in a squash-aware path that bypasses the stale-branch guard, so squash-merged sprint stories triage to skip_merged instead of full reruns."
    - criterion: "Stories whose PRs were squash-merged should be detected as merged and skipped on resume."
      status: MET
      notes: "_is_branch_merged falls back to audit-backed APPROVE detection for same-tip/squash topologies, and sprint story audits now persist to history.jsonl so resume can see prior approvals."
    - criterion: "The implementation should distinguish squash-merged stories from stale empty branches during resume."
      status: MET
      notes: "Ordinary stale-branch triage still requires topology evidence and rejects empty/no-work branches without audit history, while squash-merged branches with persisted APPROVE records are skipped as merged."
    - criterion: "Regression coverage should lock in the squash-merge resume behavior."
      status: MET
      notes: "Tests cover squash-merge topology against real audit-history behavior, the single-call guarded audit persistence path, and the corrected stale/no-worktree triage expectations."
  story_deviations: none
  deferred_items: none
  gate_result: PASS
gate_decision: PASS
