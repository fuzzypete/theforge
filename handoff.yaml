gate_decision: PASS
next_recommended_step: review
dev_notes: |
  summary: "Rewrote README.md to follow the landing funnel structure: hero/positioning,
    'Is TheForge for you?' self-sort section, 5-minute quickstart using hello-forge
    as the canonical first run, 'How it works' with the lifecycle diagram in its own
    section, 'What gets created' explaining user-authored vs generated vs safe-to-delete
    files, then configuration, documentation table, architecture, and development.
    The cost table was retained immediately after the quickstart as a trust signal.
    All existing content was preserved and reorganised — nothing was removed."

  commits:
    - sha: "c931282"
      message: "docs(readme): restructure around landing funnel"

  acceptance_criteria:
    - criterion: "README sections appear in the specified order (hero, self-sort, quickstart, how-it-works, what-gets-created, config, docs, architecture, development)"
      status: MET
      notes: "Sections appear in exactly the required order. Cost table retained between quickstart and how-it-works as a trust signal."
    - criterion: "'Is TheForge for you?' section with Best for and Not ideal for lists"
      status: MET
      notes: "Section added with all four Best for and four Not ideal for bullet points from the spec."
    - criterion: "5-minute quickstart: clone, install, check-providers, run hello-forge example, expected output summary"
      status: MET
      notes: "Steps: clone/install, check providers (with verify command), run hello-forge, expected output block, merge command."
    - criterion: "Quickstart references the hello-forge example as the canonical first run"
      status: MET
      notes: "Step 3 uses 'examples/hello-forge' and 'specs/add-greeting.md' as the first-run example."
    - criterion: "'How it works' section with lifecycle diagram and minimal prose"
      status: MET
      notes: "State machine diagram moved into dedicated 'How it works' section with one-line descriptions per phase."
    - criterion: "'What gets created' section explaining .forge/, logs, worktrees, audit outputs"
      status: MET
      notes: "New section with annotated directory tree distinguishing user-authored, generated, and safe-to-delete files."
    - criterion: "No content is lost — existing sections are reorganized, not removed"
      status: MET
      notes: "All prior sections (Why TheForge content, quickstart, cost table, docs table, config, architecture, development, license) are present."
    - criterion: "Cost table remains"
      status: MET
      notes: "Cost table retained verbatim after the quickstart section."

  story_deviations: none
  deferred_items: none
  gate_result: PASS
