dev_notes: |
  summary: "Moved the OpenAI and DeepSeek runner imports inside the openai/httpx sys.modules stub context in tests/test_runner_submit_tool_gating.py so importing the API runner module no longer raises ModuleNotFoundError in CI before the optional SDK mocks are installed. Re-audited the file for the same import-before-stub pattern and kept the existing gating assertions unchanged."
  commits:
    - sha: "e39ac6c"
      message: "fix(tests): stub optional sdk imports in submit gating tests"
    - sha: "15a3388"
      message: "fix(issue-444): rewrite dev handoff to match schema"
    - sha: "bf76789"
      message: "fix(tests): address review findings (iter 1)"
  acceptance_criteria:
    - criterion: "main CI broken — test_runner_submit_tool_gating.py imports openai/httpx without mocking"
      status: MET
      notes: "Both helper paths that load the API runner module for OpenAI-backed code now install openai/httpx stubs before importing from theforge.runners.api, covering the DeepSeek path that transitively imports optional SDK modules at module load time."
  story_deviations: none
  deferred_items: none
  gate_result: PASS
