gate_decision: FAIL
scope_completed:
  - R1: run_agent_pool uses ThreadPoolExecutor for concurrent execution
  - R2: _process_stream_event prefixes lines with [label] (e.g. '↳ [opus] Read: file.py')
  - R3: pool progress logging with accurate wall-clock vs sequential comparison
  - R4: per-agent exception isolation via try/except around future.result()
  - R5: single-agent pool bypasses ThreadPoolExecutor entirely
  - R6: 5 new tests in test_runner.py (parallel, order, single-no-thread, failure-isolated, same-prompt)
  - P2 fix: per-agent duration tracked via _timed_agent finally block (not elapsed-from-pool-start)
deferred_followups:
  - BLOCKER for gate PASS: tests/test_coordinator.py line ~1261 is OUT OF SCOPE.
    TestVerboseFlagEnablesToolLines.test_verbose_prints_tool_lines asserts '"↳ Read"'
    but _process_stream_event now outputs '↳ [test-label] Read: /foo.py'.
    Fix: change assertion to '"↳ [test-label] Read"'. One-line change.
next_recommended_step: >
  Expand file scope to include tests/test_coordinator.py, update line ~1261
  assertion from '"↳ Read"' to '"↳ [test-label] Read"', then re-run gate for PASS.
