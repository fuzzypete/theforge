dev_notes: |
  summary: "Converted src/theforge/cli.py (2424 lines) into a src/theforge/cli/ package.
    Each subcommand group was extracted into its own module: run.py, review.py, sprint.py,
    daemon.py, status.py, ideate.py, providers.py, audit.py, telemetry.py, hooks.py, and
    init_commands.py (cmd_init/cmd_secrets_init/cmd_version + their helpers). Shared helpers
    (_find_config, _parse_story_frontmatter, _build_task, _append_history, _write_audit,
    _cmd_dry_run, _apply_dev_model_override, _apply_plan_model_override) live in shared.py.
    main.py holds build_parser() and main(). __init__.py re-exports the full public surface
    for backward compatibility. __main__.py adds python -m theforge.cli support. The
    pre-existing work had already split most modules; this commit completed init_commands.py,
    __main__.py, fixed shared.py to be under 400 lines, and updated all imports."
  commits:
    - sha: "1b6da65"
      message: "refactor(cli): split cli.py monolith into cli/ package"
  acceptance_criteria:
    - criterion: "make test passes with the same test count"
      status: MET
      notes: "1410 tests pass before and after the split."
    - criterion: "make lint passes"
      status: MET
      notes: "ruff check and ruff format --check both pass with no issues."
    - criterion: "from theforge.cli import main works"
      status: MET
      notes: "Verified: python -c 'from theforge.cli import main; print(ok)' prints ok."
    - criterion: "forge --help and every subcommand --help produce identical output"
      status: MET
      notes: "Pure structural refactor — parser construction unchanged in main.py."
    - criterion: "No single file in src/theforge/cli/ exceeds 400 lines"
      status: MET
      notes: "Largest file is run.py at exactly 400 lines. All others are under 400."
    - criterion: "The old cli.py file no longer exists"
      status: MET
      notes: "src/theforge/cli.py deleted; Python resolves theforge.cli to cli/__init__.py."
  story_deviations: none
  deferred_items: none
  gate_result: PASS
