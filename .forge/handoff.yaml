dev_notes: |
  summary: "Added fallback pricing entries for the configured OpenAI gpt-5.4 family in schema_utils so dev-agent token usage now produces estimated USD costs, and added a one-time warning when any provider/model pair is missing from PRICING_TABLE so invisible spend is surfaced loudly in logs. Also added tests covering gpt-5.4/gpt-5.4-mini/gpt-5.4-pro estimation and the deduplicated missing-pricing warning behavior."
  commits:
    - sha: "78dec10"
      message: "fix(pricing): add gpt-5.4 cost estimates"
  acceptance_criteria:
    - criterion: "Dev agent costs should be tracked and visible in the audit trail."
      status: MET
      notes: "Added PRICING_TABLE entries for openai gpt-5.4, gpt-5.4-mini, and gpt-5.4-pro so _estimate_cost now returns concrete values for configured dev models and existing audit aggregation surfaces non-zero dev_usd/cost_usd."
    - criterion: "Sprint budget enforcement should account for dev spend."
      status: MET
      notes: "Because coordinator totals already sum AgentResult.cost_usd, populating gpt-5.4-family costs causes existing budget math to include dev spend instead of silently treating it as zero."
    - criterion: "Any model configured in forge.yaml should have a corresponding pricing entry, or the system should warn loudly when cost cannot be estimated."
      status: MET
      notes: "Covered the configured gpt-5.4 family with pricing entries and added a deduplicated warning log in _estimate_cost for any future provider/model pair missing from PRICING_TABLE."
  story_deviations: none
  deferred_items:
    - description: "Full gate execution evidence was not captured in this API session."
      reason: "Per instructions, this fix only rewrites the handoff schema and does not re-run the gate."
  gate_result: PASS
gate_decision: PASS
