gate_decision: PASS
next_recommended_step: review
dev_notes: |
  summary: "Four targeted P2 bug fixes across coord_state.py, task.py, and coord_audit.py.
    (1) Added total_story_validation_cost property to CoordinatorState and included it in
    total_cost, using explicit is not None checks per the plan reviewer's P2 advisory about
    the or 0.0 anti-pattern. (2) Replaced lstrip('-') with [3:] in task.py to strip the
    YAML '---' document marker as a three-character token rather than a character set,
    preventing corruption of values like '-home'. (3) Removed the inner ternary in
    coord_audit.py that produced None for an empty findings list — plan_validation now always
    emits a block when plan_structured is set (skipped=False, findings=[]) rather than None,
    distinguishing clean pass from validation-not-run. (4) Replaced three or 0.0 sites with
    explicit is not None checks in coord_audit.py:162, coord_audit.py:187, and
    coordinator.py:696. One existing test in test_coord_preflight.py was updated to include
    story_validation cost in its total_cost assertion — the prior assertion was stale."

  commits:
    - sha: "6d45a4a"
      message: "fix(cost,audit,task): four P2 bug fixes — cost tracking, plan parsing, audit shape"

  acceptance_criteria:
    - criterion: "total_cost includes spec_validation cost when present"
      status: MET
      notes: "Added total_story_validation_cost property; included in total_cost sum. Uses
        explicit is not None checks consistent with Step 4 fix (following plan reviewer P2 note)."
    - criterion: "YAML document marker detection uses string comparison, not lstrip"
      status: MET
      notes: "task.py: stripped = stripped[3:].strip() — removes exactly three characters.
        The startswith('---') guard on line 71 already confirms the prefix exists."
    - criterion: "Plan validation audit block present with empty findings on clean pass, absent only when validation did not run"
      status: MET
      notes: "Removed inner ternary returning None for empty list. When plan_structured is
        not None the block always emits skipped=False and findings=[]. Outer guard unchanged."
    - criterion: "Duration fields use is not None checks instead of or 0.0"
      status: MET
      notes: "Fixed the three sites flagged in preflight evidence: coord_audit.py:162
        (reviewer cost accumulation), coord_audit.py:187 (preflight_duration_s in totals),
        coordinator.py:696 (per-profile budget sum). Additional or 0.0 sites in
        coord_state.py cost properties were noted by plan reviewer as outside story AC
        scope; deferred."
    - criterion: "All existing tests pass"
      status: MET
      notes: "1179 tests pass. One pre-existing test assertion in test_coord_preflight.py
        updated to account for story_validation cost now correctly included in total_cost."
    - criterion: "New tests for each fix"
      status: MET
      notes: "test_coord_state.py: TestTotalCostIncludesStoryValidation (4 cases including
        zero and None cost_usd). test_task.py: TestParsePlanOutputYamlMarker (3 cases
        including dash-in-value regression). test_coord_audit.py: TestPlanValidationAuditBlock
        (3 cases: clean pass, skipped, populated) + TestDurationAndCostNoneChecks (2 cases:
        zero duration preserved, zero-cost reviewer appears in per_reviewer)."

  story_deviations: none

  deferred_items:
    - description: "coord_state.py total_dev_cost / total_review_cost / total_preflight_cost
        / total_plan_cost / total_plan_review_cost still use or 0.0"
      reason: "Story AC scopes fix to 'duration fields'; plan reviewer noted these as P2
        advisory only. Deferred to a follow-on cleanup story."

  gate_result: PASS
