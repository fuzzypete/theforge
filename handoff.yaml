gate_decision: PASS
dev_notes: |
  summary: "Fixed the escalation note delivery on the reject-after-escalation path. When
    persistent P1 escalation fires and the human subsequently rejects on exhausted cycles,
    the retry goes through build_dev_prompt (not build_fix_prompt), which previously did not
    accept or render the escalation note. Added escalation_note param back to build_dev_prompt,
    rendered unconditionally (not gated on review_findings), and passed state.escalation_note
    at the call site in _run_dev_phase so the upgraded dev model is always told why it was
    escalated. Added an integration test covering persistent P1 + exhausted cycles + human
    reject, asserting the escalation note appears in the subsequent dev prompt."
  commits:
    - sha: "c319b5f"
      message: "fix(cycle-aware-feedback): address review findings (iter 1)"
  acceptance_criteria:
    - criterion: "CoordinatorState accumulates review cycle summaries (not just raw ReviewResult objects — a lightweight history)"
      status: MET
      notes: "CycleHistory dataclass; cycle_history + cycle_history_total fields; all review paths append history"
    - criterion: "build_fix_prompt includes a 'Previous Cycles' section showing what was flagged and attempted in earlier cycles"
      status: MET
      notes: "build_fix_prompt renders Previous Review Cycles section when cycle_history is non-empty"
    - criterion: "When no previous cycles exist (first review), no history section appears"
      status: MET
      notes: "Section only rendered when cycle_history is non-empty"
    - criterion: "When model escalation fires, the dev prompt includes an explicit note about the escalation and which finding triggered it"
      status: MET
      notes: "escalation_note set on persistent P1; rendered in build_fix_prompt and build_dev_prompt; delivered even on reject-after-escalation path"
    - criterion: "Cycle history is capped at 3 most recent cycles to bound prompt size"
      status: MET
      notes: "_append_cycle_history trims to last 3; cycle_history_total counter keeps numbering monotonic after trimming"
    - criterion: "review_to_dev_handoff output is unchanged (reviewer->dev format stays)"
      status: MET
      notes: "review_to_dev_handoff not modified; existing tests confirm no regression"
    - criterion: "All existing tests pass"
      status: MET
      notes: "673 tests pass"
  spec_deviations: none
  deferred_items: none
  gate_result: PASS
