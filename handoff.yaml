gate_decision: PASS
scope_completed:
  - "SpecTriage dataclass in sprint.py"
  - "_triage_spec() function with 4 actions: skip_merged, review, dev, full"
  - "_read_prior_sprint_cost() for cost continuity from prior audit"
  - "run_from_dev() in coordinator.py - DEV entry on existing worktree"
  - "run_sprint() resume parameter with triage routing"
  - "--resume flag added to forge sprint CLI subcommand"
  - "9 new tests: 5 triage unit tests + 4 resume integration tests"
deferred_followups: []
next_recommended_step: "forge review specs/sprint-resume.md"
