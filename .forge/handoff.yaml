dev_notes: |
  summary: "Restored branch-protection-aware merge-pr cleanup so queued auto-merge preserves the remote branch until GitHub completes the merge, while immediate merges still delete the remote branch; also refreshed the notes for this review iteration."
  commits:
    - sha: "726a1cc"
      message: "fix(coordinator): address review findings (iter 1)"
    - sha: "261e7ea"
      message: "fix(issue-438): rewrite dev handoff to match schema"
    - sha: "52179cc"
      message: "fix(coordinator): address review findings (iter 1)"
    - sha: "e4c7262"
      message: "fix(coordinator): auto-merge PRs behind branch protection"
  acceptance_criteria:
    - criterion: "`merge-pr` should use `--auto` when branch protection is enabled, allowing GitHub to merge once checks pass. Alternatively, wait for CI status before attempting merge."
      status: MET
      notes: "`_merge_pr` now preserves the remote feature branch when `gh pr merge --auto` queues auto-merge under branch protection, while still deleting the remote branch after immediate merges; this satisfies the branch-protection-aware merge behavior required by the spec."
  story_deviations: none
  deferred_items: none
  gate_result: PASS
gate_decision: PASS
