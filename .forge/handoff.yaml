deferred_items: none
dev_notes: |
  summary: "Implemented GitHub blocker-aware sprint dependency analysis end to end: milestone query mode still infers `depends_on` from GitHub \"blocked by\" relationships, and the follow-up fixes aligned runtime and `--dry-run` handling of unresolved out-of-sprint blockers so blocked stories are classified before DAG construction and downstream in-sprint blockage is propagated instead of crashing."
  commits:
    - sha: "9105834"
      message: "fix(sprint): address review findings (iter 1)"
    - sha: "2911b4d"
      message: "summary: \"Aligned sprint query dry-run dependency analysis with runtime"
    - sha: "36b6f08"
      message: "fix(sprint): align dry-run blocker handling"
    - sha: "b339ca2"
      message: "fix(sprint): address review findings (iter 1)"
    - sha: "adc63c0"
      message: "fix(sprint): address review findings (iter 1)"
    - sha: "66b3ef1"
      message: "fix(issue-294): rewrite dev handoff to match schema"
    - sha: "13a54bc"
      message: "summary: \"Implemented GitHub blocker inference for sprint query mode by"
  acceptance_criteria:
    - criterion: "When issue B is \"blocked by\" issue A in GitHub, `forge sprint --milestone` treats B as `depends_on: [issue-A]`"
      status: MET
      notes: "GitHub issue loading still maps blocker relationships into `TaskStory.depends_on` using `issue-NNN` slugs."
    - criterion: "With `--parallel N`, A and B never run concurrently; B starts only after A completes and merges"
      status: MET
      notes: "Runtime normalizes unresolved external blockers before DAG construction, so blocked stories are withheld and dependent stories cannot start early."
    - criterion: "Stories with no blocker relationships are treated as independent (no change to current behavior)"
      status: MET
      notes: "Independent stories continue through the existing scheduler unchanged when they have no inferred or manifest dependencies."
    - criterion: "`--dry-run` output reflects the dependency-aware ordering"
      status: MET
      notes: "Dry-run now mirrors runtime treatment of external blockers and marks downstream blocked stories instead of crashing on missing batch assignments."
    - criterion: "Sprint audit YAML includes `inferred_dependencies` showing which depends_on came from GH blockers vs manifest"
      status: MET
      notes: "Audit output remains in place and continues to distinguish GitHub-inferred dependencies from manifest-defined ones."
    - criterion: "No change to behavior when `--parallel` is not set (sequential is already correct)"
      status: MET
      notes: "Sequential execution keeps the same flow, aside from correctly classifying unresolved out-of-sprint blockers instead of failing unexpectedly."
  story_deviations: none
  deferred_items: none
  gate_result: PASS
gate_decision: PASS
gate_result: PASS
story_deviations: none
