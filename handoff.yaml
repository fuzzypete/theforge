gate_decision: PASS
scope_completed:
  - "_is_stale_worktree(path, base_branch, config) -> bool: returns True when 0
    commits ahead of base, last commit older than stale_worktree_days, or branch
    missing"
  - "_remove_worktree(path, branch): best-effort git worktree remove --force + git
    branch -D with warning logs"
  - "_setup_workspace() staleness check: detects existing worktree, calls _is_stale_worktree,
    removes or reuses with clear log output"
  - "WorkspaceConfig.stale_worktree_days: int = 1 field added (stale_worktree_days:
    0 always removes)"
  - "TestStaleWorktree: 7 tests covering no-existing-worktree, zero-commits-ahead,
    old-commit, fresh-reuse, days-zero, warning-logging, remove-failure-no-raise"
  - "style: committed ruff formatting changes to ideate.py, test_cli.py, test_ideate.py
    (outside file_scope, generated by make fmt)"
deferred_followups: []
next_recommended_step: "Submit for review — all 267 tests pass, stale worktree detection implemented"
